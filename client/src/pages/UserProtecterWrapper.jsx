import React from 'react'
import {UserDataContext} from '../context/UserContext'

const UserProtecterWrapper = () => {

    const {user,setUser}=useContext(UserDataContext)
  return (
    <div>
      
    </div>
  )
}

export default UserProtecterWrapper
